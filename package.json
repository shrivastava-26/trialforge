{
  "name": "trialforge",
  "version": "0.1.0",
  "private": true,
  "description": "TrialForge — modular clinical-trial management platform",
  "scripts": {
    "dev": "echo \"TrialForge Dev Commands:\" && echo \"  npm run dev:site-network:frontend  — Start SNA frontend\" && echo \"  npm run dev:site-network:backend   — Start SNA backend\" && echo \"  npm run test:site-network           — Run all SNA tests\" && echo \"  npm run e2e:site-network            — Run SNA Playwright e2e\"",
    "dev:site-network:frontend": "cd modules/site-network/frontend && npm install && npm run dev",
    "dev:site-network:backend": "cd modules/site-network/backend && npm install && npm run dev",
    "test:site-network": "cd modules/site-network && npm run test:backend && npm run test:frontend",
    "test:site-network:backend": "cd modules/site-network && npm run test:backend",
    "e2e:site-network": "cd modules/site-network && npm run test:e2e",
    "dev:identity:backend": "cd modules/identity/backend && npm install && npm run dev",
    "test:identity": "cd modules/identity/backend && npm test",
    "test:identity:backend": "cd modules/identity/backend && npm test",
    "dev:patient-registry:backend": "cd modules/patient-registry/backend && npm install && npm run dev",
    "test:patient-registry": "cd modules/patient-registry/backend && npm test",
    "test:patient-registry:backend": "cd modules/patient-registry/backend && npm test"
  },
  "keywords": ["clinical-trial", "ctms", "graphql", "react", "typescript"],
  "license": "UNLICENSED"
}
